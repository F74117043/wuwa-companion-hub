import { PageFrame } from "@/components/PageFrame";

export default function CharactersPage() {
  return (
    <PageFrame
      eyebrow="Resonator archive"
      title="Characters"
      description="A scalable route for roster browsing, profile pages, roles, elements, and future team composition tools."
    />
  );
}
