import { PageFrame } from "@/components/PageFrame";

export default function LorePage() {
  return (
    <PageFrame
      eyebrow="Archive signal"
      title="Lore"
      description="A future visual map for Wuthering Waves story arcs, regions, factions, and character relationships."
    />
  );
}
