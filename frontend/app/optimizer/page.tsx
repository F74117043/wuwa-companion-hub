import { PageFrame } from "@/components/PageFrame";

export default function OptimizerPage() {
  return (
    <PageFrame
      eyebrow="Build intelligence"
      title="AI Optimizer"
      description="A modular route for future weapon, echo, stat, and build recommendation workflows."
    />
  );
}
